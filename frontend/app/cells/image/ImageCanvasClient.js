'use client';

import { useContext } from 'react';
import { EnvContext } from "../../../lib/contexts/EnvContext";

const ImageCanvasClient = ({ value, query, columnName, expanded }) => {
    return <>CANVAS</>
}

export default ImageCanvasClient;