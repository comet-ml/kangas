import config from '../config';


const Page = ({ searchParams }) => {
    const { 
        dgid,
        filter,
        groupBy,
        sortBy,
        sortDesc 
    } = searchParams;

    const 
}